if [[ -n "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.8.7-p302@hitch" ]] ; then
  \. "$rvm_path/environments/ruby-1.8.7-p302@hitch"
else
  rvm --create use  "ruby-1.8.7-p302@hitch"
fi